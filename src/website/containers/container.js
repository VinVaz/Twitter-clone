import { connect } from 'react-redux';
import App from '../App';
import {
  createTweet,
  searchTwitter,
  showUnloggedPage,
  showLoginPage,
  showLoggedPages,
  showTweets,
  showFollowing,
  showFollowers,
  showHomePage,
  likeTweet,
  replyTweet,
  retweet,
  followSomeone,
  showThread,
  handleNewTweets,
  handleProfileBox,
  showBackground,
  showCreateNewTweet,
} from '../actions';

const mapStateToProps = state => ({
  tweet: state.tweet,
  search: state.search,
  likeButtonPressed: state.interaction.likeButtonPressed,
  replyTweetPressed: state.interaction.replyTweetPressed,
  retweetPressed: state.interaction.retweetPressed,
  followButtonPressed: state.interaction.followButtonPressed,
  unloggedPageShowing: state.view.mainPages.unloggedPageShowing,
  loginPageShowing: state.view.mainPages.loginPageShowing,
  loggedPagesShowing: state.view.mainPages.loggedPagesShowing,
  followingShowing: state.view.getUsers.followingShowing,
  followersShowing: state.view.getUsers.followersShowing,
  homePageShowing: state.view.getUsers.homePageShowing,
  tweetsShowing: state.view.getUsers.tweetsShowing,
  threadShowing: state.view.loggedPage.threadShowing,
  newTweetsButtonShowing: state.view.loggedPage.newTweetsButtonShowing,
  profileBoxShowing: state.view.loggedPage.profileBoxShowing,
  backgroundIsHidden: state.view.loggedPage.backgroundIsHidden,
  createNewTweetIsShowing: state.view.loggedPage.createNewTweetIsShowing,
});
const mapDispatchToProps = dispatch => ({
  createTweet: text => dispatch(createTweet(text)),
  searchTwitter: text => dispatch(searchTwitter(text)),
  showTweets: () => dispatch(showTweets()),
  showHomePage: () => dispatch(showHomePage()),
  showFollowing: () => dispatch(showFollowing()),
  showFollowers: () => dispatch(showFollowers()),
  likeTweet: () => dispatch(likeTweet()),
  replyTweet: () => dispatch(replyTweet()),
  retweet: () => dispatch(retweet()),
  followSomeone: () => dispatch(followSomeone()),
  showThread: () => dispatch(showThread()),
  handleNewTweets: () => dispatch(handleNewTweets()),
  handleProfileBox: () => dispatch(handleProfileBox()),
  showBackground: () => dispatch(showBackground()),
  showCreateNewTweet: () => dispatch(showCreateNewTweet()),
  showLoginPage: () => dispatch(showLoginPage()),
  showLoggedPages: () => dispatch(showLoggedPages()),
  showUnloggedPage: () => dispatch(showUnloggedPage()),
});

export default connect(
  mapStateToProps,
  mapDispatchToProps,
)(App);
